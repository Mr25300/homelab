services:
  server:
    image: itzg/minecraft-server:latest
    labels:
      mc-router.host: records-armor.gl.joinmc.link,records-armor.gl.at.ply.gg
    environment: # https://docker-minecraft-server.readthedocs.io/en/latest/variables/#rcon
      EULA: "TRUE"
      TYPE: "AUTO_CURSEFORGE"
      CF_API_KEY: ${CF_API_KEY}
      CF_MODPACK_MANIFEST: /manifests/manifest.json
      CF_SLUG: "custom"

      INIT_MEMORY: "8G"
      MAX_MEMORY: "11G"
      VIEW_DISTANCE: "12"
      SIMULATION_DISTANCE: "12"

      MOTD: "All The Mods Server"
      ENABLE_WHITELIST: "true"
      WHITELIST: "AwesomeFeet313,AKAPVP66,Ginormous_G,shubabidi,Mineor23"
      OPS: "AwesomeFeet313"
      EXISTING_WHITELIST_FILE: "MERGE"
      EXISTING_OPS_FILE: "MERGE"

      ALLOW_FLIGHT: "true"
    volumes:
      - ./data:/data
      - ./manifests:/manifests:ro
      - ./overrides/config:/config:ro
    restart: no
    stdin_open: true
    tty: true
    deploy:
      resources:
        limits:
          memory: 12G
          cpus: '4'
    networks:
      - mc-net

networks:
  mc-net:
    external: true
