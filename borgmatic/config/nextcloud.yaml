<<: !include shared/defaults.yaml

constants:
  service: nextcloud

source_directories:
  - /mnt/source/nextcloud

exclude_patterns:
  - /mnt/source/nextcloud/db

repositories:
  - path: /mnt/repos/sagittarius-nextcloud
    label: "sagittarius-nextcloud-local"

  - path: ${BORGBASE_NEXTCLOUD_REPO}
    label: "sagittarius-nextcloud-borgbase"
